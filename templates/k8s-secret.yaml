{{- $vmTemplates := default (list) .Values.k8sTemplateParams.k8sVmTemplate }}
{{- if eq (len $vmTemplates) 0 }}
{{- fail "k8sTemplateParams.k8sVmTemplate must contain at least one item when rendering secrets" }}
{{- end }}
{{- $vmSecretParams := index $vmTemplates 0 }}
{{- range $index, $ns := .Values.k8snamespaces }}
{{- if not $ns.name }}
{{- fail (printf "k8snamespaces[%d].name is required" $index) }}
{{- end }}
{{- if not $ns.edgeHostToken }}
{{- fail (printf "k8snamespaces[%d].edgeHostToken is required" $index) }}
{{- end }}
apiVersion: v1
kind: Secret
metadata:
  name: v-k8s-cloudinit
  namespace: {{ $ns.name | quote }}
type: Opaque
stringData:
  userdata: |
    #cloud-config
    ssh_pwauth: True
    chpasswd: { expire: False }
    password: {{ required "k8sTemplateParams.k8sVmTemplate[0].password is required" $vmSecretParams.password }}
    disable_root: false
    packages:
      - jq
      - conntrack
    write_files:
      - path: /tmp/user-data
        owner: 'root:root'
        permissions: '0755'
        content: |
          #cloud-config
          install:
            reboot: true
            poweroff: false
          stylus:
            skipKubeVip: false
            site:
              edgeHostToken: {{ $ns.edgeHostToken }}
              paletteEndpoint: {{ required "k8sTemplateParams.k8sVmTemplate[0].paletteEndpoint is required" $vmSecretParams.paletteEndpoint }}
              tags:
                name: %NAME%
    runcmd:
      - apt-get update
      - apt-get install -y qemu-guest-agent
      - systemctl start qemu-guest-agent
      - |
        mkdir -p /system/oem; mkdir -p /oem; mkdir -p /usr/local/cloud-config; mkdir -p /opt/spectrocloud/state
        curl --location --output /tmp/palette-agent-install.sh https://github.com/spectrocloud/agent-mode/releases/latest/download/palette-agent-install.sh
        chmod +x /tmp/palette-agent-install.sh
        export USERDATA=/tmp/user-data
        sed -i "s/%NAME%/$(hostname)/" /tmp/user-data
        sudo --preserve-env /tmp/palette-agent-install.sh
---
{{- end }}
